# Product Specs Index

This repository currently has one active product direction:

- HTTPS chat on `thefem.ru` with persistent cross-session memory.

As the Telegram path is deprioritized, new product behavior should be specified here before transport-specific code expands again.
